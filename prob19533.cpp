#include <bits/stdc++.h>

using namespace std;
int sMap[100100][1000];

void init(){
    int num = 3;
    while(num < 100009){
        int t = 0;
        for(int j = 0; j < 4; j++)
            for(int i = t; i < t + 1; i++){

            }
    }

}

int main(){
    ios_base::sync_with_stdio(0);
    memset(sMap, -1, sizeof(sMap));

}